{% extends 'base.html.twig' %}
{% block title %}Contactez-nous{% endblock %}

{% block body %}
<div class="row justify-content-center mt-5">

<h1>Prendre contact avec Terres et Cultures Solidaires</h1>
<h1>Entretien des compétences</h1>
<div class="col-6">
<div class="bg-white p-5 contact-form">
    <div class="form-group">
{{ form_start(reservation_form) }}
    {{ form_row(reservation_form.fullName, {'attr': {'class': 'form-control'}}) }}


</div>
<div class="form-group">
    {{ form_row(reservation_form.email, {'attr': {'class': 'form-control'}}) }}

</div>
<div class="form-group">
    {{ form_row(reservation_form.message, {'attr': {'class': 'form-control'}}) }}

</div>

<div class="form-group">
    <input type="submit" value="Send" class="btn btn-primary py-3 px-5">
</div>
{{ form_end(reservation_form) }}
</div>
</div>
</div>
{% endblock %}